import { HeaderDirective } from './header-links.service';

describe('HeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderDirective();
    expect(directive).toBeTruthy();
  });
});
